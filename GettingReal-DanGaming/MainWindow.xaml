<Window x:Class="GettingReal_DanGaming.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GettingReal_DanGaming.ViewModels"
        mc:Ignorable="d"
        Title="DanGaming Lageroversigt" Height="450" Width="800"
        Icon="/Images/LOGOSINGLE.png">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Source="/Images/DangamingLogo.png" 
                   HorizontalAlignment="Left" 
                   Height="50"/>

            <Button Grid.Column="1" 
                    Content="Login"
                    Padding="15,5"
                    VerticalAlignment="Center" Click="Button_Click"/>
        </Grid>

        <Rectangle Grid.Row="0" VerticalAlignment="Bottom" Height="1" Fill="Black"/>


        <Grid Grid.Row="1" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" Margin="0,0,20,0">
                <TextBlock Text="Produktoversigt" FontWeight="Bold" Margin="0,0,0,5"/>
                <Grid Grid.Column="0" Margin="0,0,20,0">

                </Grid>
                <Grid Width="200" HorizontalAlignment="Left">

                    <TextBox x:Name="SearchTextBox" 
             Padding="5"
             VerticalContentAlignment="Center"/>

                    <TextBlock Text="🔍 Søg efter produkt"
               IsHitTestVisible="False"
               Foreground="Gray"
               VerticalAlignment="Center"
               Margin="7,0,0,0">

                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Hidden"/>

                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding ElementName=SearchTextBox, Path=Text.Length}" Value="0" />
                                            <Condition Binding="{Binding ElementName=SearchTextBox, Path=IsKeyboardFocused}" Value="False" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Visibility" Value="Visible" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>
            </StackPanel>

            <Button x:Name="AddProductButton"
                    Grid.Column="1"
                    Content="Tilføj nyt produkt" 
                    VerticalAlignment="Bottom"
                    Padding="10,5"
                    IsEnabled="false"/>
        </Grid>


        <ListBox x:Name="WarehouseListBox"
                 Grid.Row="2"
                 BorderThickness="1"/>
        <DataGrid 
            Grid.Row="2" 
            BorderThickness="1"
            BorderBrush="Black"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            IsReadOnly="True"
            x:Name="ProductDataGrid"
            ItemsSource="{Binding ProductsVM}">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produktnavn" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Mærke" Binding="{Binding Brand}" Width="*"/>
                <DataGridTextColumn Header="Pris" Binding="{Binding Price}" Width="80"/>
                <DataGridTextColumn Header="Kategori" Binding="{Binding CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Underkategori" Binding="{Binding SubcategoryName}" Width="*"/>
                <DataGridTextColumn Header="Antal" Binding="{Binding Quantity}" Width="50"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>